# SSOT manifest for self-hosted apps on bitbaum (167.233.22.31).
#
# Format:
#   name|port|domains|repo_path|app_dir|db|owner|kind|status|plan|price|since
#
#   name..db   provisioning — what sync-infra.sh and deploy.sh read (unchanged)
#   db         host Postgres database name, OR `supabase:<schema>` for an app
#              living in the supabase-db container, OR '-' for no database.
#              One supabase database backs several apps, so each gets its OWN
#              schema: `public` belongs to orangecat, and a second app's
#              migrations landing there would collide with it by table AND by
#              function name. '-' skips the schema step entirely — which is
#              how botsmann ran for months against 11 migrations nobody had
#              applied, serving 503 from a table that did not exist.
#   owner      who this belongs to. 'bitbaum' = ours. Otherwise the client.
#   kind       product | client-app | client-site | demo | infra | library
#   status     live | prospect | validating | demo | unverified | handed-over | archived
#   plan/price what they pay, and how much. '-' means NOT KNOWN — and the gaps
#              are the point: right now "what am I owed this month" and "what am
#              I maintaining for free" are unanswerable, which is why these
#              columns exist before the numbers do.
#              `favour|0` is a RECORDED ANSWER, not a placeholder: unpaid on
#              purpose. It is the opposite of '-', which means nobody knows.
#              On 2026-09-10 George confirmed all four live client engagements
#              are favours — "nobody pays anything yet" — so the four '-|-'
#              pairs became `favour|0` and the ledger baseline went 4 -> 0.
#              The "yet" is the whole point of writing it down: the next live
#              client now has to arrive with terms instead of silently becoming
#              the fifth favour.
#              Read by scripts/ci/check-client-ledger.sh, which ratchets the
#              number of LIVE client engagements still carrying '-'. The columns
#              alone did not work: they made the gap recordable, nothing ever
#              read them, and eight months of '-' looked exactly like eight
#              months of having no clients. A new live client engagement now has
#              to arrive with terms; the four that predate the gate are the
#              baseline it counts down from.
#   since      when the relationship started (ISO date), '-' if unrecorded.
#
# One register, not four. bitbaum/projects/*.md holds the narrative; this holds
# the facts, because this is the file that provisioning reads and therefore the
# only one nobody can afford to let go stale.
#
# domains "-" = internal only. Ports 4001-4004 are the pre-existing handcrafted
# services (bridge, loki, orangecat; evig joined the register on 2026-09-11) — they keep their own units
# and Caddy blocks and are NOT listed here.
kivvi|4005|kivvi.orangecat.ch|/home/g/dev/kivvi|apps/web|kivvi|RevampIT|client-app|live|favour|0|-
# datacat is a full-stack Next app (Prisma + tRPC API routes); the express
# backend/ dir is local-dev legacy and is not deployed.
datacat-web|4006|datacat.orangecat.ch|/home/g/dev/datacat|frontend|datacat|bitbaum|product|live|-|-|-
# AOZ Begleitung — the product name (SSOT: aoz-begleitung src/lib/config/brand.ts).
# `aoz-wohnen` here, the systemd unit, /opt/aoz-wohnen and the aoz_wohnen
# database keep the housing-era name on purpose: they are addresses nobody
# reads, and renaming them is a migration, not a correction.
#
# The REPO was bitbaum/aoz-housing — the last place the product was still called
# Housing — and was renamed to bitbaum/aoz-begleitung on 2026-09-15 because it is
# PUBLIC: the retired name sat on the org page and in every clone URL. Two names
# now, not three, and this row's repo_path is why the fourth field had to move
# with it: repo-metadata-audit.mjs keys the register on its BASENAME, so while it
# said aoz-housing the join against GitHub missed and every check for this repo
# was silently skipped — the audit passed by not looking. See fleet
# registers/retired.json for the rename, and do not infer the product from the
# deploy name.
#
# The canonical public host is aoz.orangecat.ch, and until 2026-09-10 this row
# said aoz-wohnen.orangecat.ch — which is only a 301 to it. That was not a
# cosmetic error: sync-infra.sh rewrites /etc/caddy/apps.d/<name>.caddy from
# this field with an unconditional `tee`, so the next sync would have deleted
# the aoz.orangecat.ch site block outright and taken the client's real URL off
# the air. The register has to name the host that actually serves.
#
# aoz-wohnen.orangecat.ch is still redirected on the box (it is printed on paper
# and bookmarked by residents). That redirect currently lives INSIDE the
# generated file, so a sync still drops it — it belongs in its own
# aoz-legacy-redirect.caddy, the way sink-legacy-redirect.caddy already does it.
aoz-wohnen|4008|aoz.orangecat.ch|/home/g/dev/aoz-begleitung|.|aoz_wohnen|AOZ|client-app|live|favour|0|2026-08-13
surf-your-life|4009|surf-your-life.orangecat.ch|/home/g/dev/surf-your-life|.|surf_your_life|bitbaum|product|live|-|-|-
vitareba|4011|vitareba.orangecat.ch|/home/g/dev/vitareba|.|vitareba|clinic|client-app|live|favour|0|-
# hirnli.orangecat.ch is the SAME app (one codebase, one deploy — the middleware
# reads the Host header to pick the tenant chrome), but it is NOT listed here:
# it is served by a hand-maintained /etc/caddy/apps.d/hirnli.caddy. Listing it
# would make sync-infra emit it a second time inside revamp-info.caddy, and two
# site blocks for one address fails `caddy validate` — which sync-infra runs
# immediately before `systemctl reload`, so the reload aborts and every OTHER
# app's pending vhost change silently fails to apply with it. This field is
# provisioning input, not documentation; a host that must not be generated does
# not belong in it.
revamp-info|4012|revamp-info.orangecat.ch,hirnli.orangecat.ch|/home/g/dev/hirnli|.|hirnli|bitbaum|product|live|-|-|-
petvity|4013|petvity.orangecat.ch|/home/g/dev/petvity|.|petvity|bitbaum|product|live|-|-|-
# botsmann.com has no nameservers (lapsed?) — serving on the orangecat.ch
# subdomain until the domain is restored; then add botsmann.com back here.
botsmann|4014|botsmann.orangecat.ch|/home/g/dev/botsmann|.|supabase:botsmann|bitbaum|product|validating|-|-|-
printcraft|4015|printcraft.orangecat.ch|/home/g/dev/printcraft|app|supabase:printcraft|-|client-app|prospect|-|-|-
# The SBB name is deliberate: this demo is being pitched TO SBB, so it is
# named after the prospect. A 2026-09-11 rename to nordbahn-fundbuero was
# reverted the same day — the repo's README flagged the name as "an open
# question, not a settled decision", and settling it threw away the point of
# the build. Nordbahn stays what it always was: the neutral DEFAULT tenant in
# lib/tenant.ts, with the SBB livery opt-in and noindex.
sbb-fundbuero|4016|sbbfundbuero.orangecat.ch|/home/g/dev/sbb-fundbuero|.|-|-|demo|demo|-|-|-
reparaturbonus-zh|4017|reparaturbonus.orangecat.ch|/home/g/dev/reparaturbonus-zh|.|reparaturbonus|Stadt Zürich?|client-app|unverified|-|-|-
# formular-erfassung: source repo location is unknown (no local copy, no
# linked GitHub repo) — not migrated.
# formular-erfassung|4018|formular.orangecat.ch|/home/g/dev/formular-erfassung|.|-
solon|4018|solon.orangecat.ch|/home/g/dev/solon|.|solon|bitbaum|product|live|-|-|-
sink|4019|sinktattoo.com,www.sinktattoo.com|/home/g/dev/s-ink|.|-|S-Ink|client-site|live|favour|0|-
# Substrata — open-source research firm (materials chokepoints). Its own repo
# and its own deploy; the orangecat.ch subdomain is an address, not an
# affiliation, and moves to its own domain when one is bought.
substrata|4022|substrata.orangecat.ch|/home/g/dev/substrata|.|substrata|bitbaum|product|live|-|-|2026-08-27
# Camille — fictional bakery, the Site Factory demo. Ran in production from
# 2026-08-19 with no repository and no entry here; both added 2026-08-27 during
# the handover rehearsal, which is exactly what the rehearsal was for.
camille-boulangerie|4020|camille.orangecat.ch|/home/g/dev/camille-boulangerie|.|-|bitbaum|demo|demo|-|-|2026-08-19
diplodoctor|4024|diplodoctor.orangecat.ch|/home/g/dev/diplodoctor|.|-|bitbaum|product|prospect|-|-|2026-09-10
# heidi serves 200 at heidi.orangecat.ch and has since it launched; `prospect`
# said the opposite, and the studio site rendered that word on its page. `live`
# in this column means the process is SERVED — it is not a release claim, and
# nothing in this fleet makes one (fleet AGENTS.md, "Names agents must share":
# everything is beta, OrangeCat included).
heidi|4025|heidi.orangecat.ch|/home/g/dev/heidi|.|heidi|bitbaum|product|live|-|-|2026-09-10
causius|4026|causius.orangecat.ch|/home/g/dev/causius|.|-|bitbaum|product|validating|-|-|2026-09-10
# evig: served from the main Caddyfile (not apps.d) on 4004; its own deploy workflow applies its drizzle migrations to the host db named here; deploy.sh never runs for it.
evig|4004|evig.orangecat.ch,revampit.orangecat.ch|/home/g/dev/evig|.|revampit|bitbaum|product|live|-|-|-
skif|4027|skif.orangecat.ch|/home/ubuntu/dev/skif|.|skif|bitbaum|product|prospect|-|-|2026-09-24
farmhouse|4029|farmhouse.orangecat.ch|/home/ubuntu/dev/farmhouse|.|-|bitbaum|client-site|prospect|-|-|2026-09-26
probe-loop2|4030|probe-loop2.orangecat.ch|/home/ubuntu/dev/probe-loop2|.|-|bitbaum|client-site|prospect|-|-|2026-09-26
